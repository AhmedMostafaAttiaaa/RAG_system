

We are using fastapi here becuase when we gonna call a function, we will use it (the function) as/from an API. So everyone can also use it

- we we're converting our code to be an acsseable from API: ![Likes this: check on URL](image.png) + ![the function](image-1.png)


# OPEN AI DOCS => how to acsses : ![check URL here](image-2.png)
Uvicorn runs the fastapi as a webserver, so we've used it



- for dev not production   
-----------------------------------
-----------------------------------
we can use this command : {uvicorn main:app --reload} instead of "uvicorn main:app" so it can reload the app everytime with our CONTROL + C
we can also use : uvicorn main:app --reload --host 0.0.0.0 ===== so everyone could use my url 

----------------------------------------------------------------------------

uvicorn main:app --reload --host 0.0.0.0 --port 5000 =>>>>> rto run the main on another port (5000) === > ![Look, there is no (/welcome) == that's because I've chance the app/base_router](image-3.png)

-------------------------------

base_router = APIRouter(
    prefix="/api/v1", ==== "http://127.0.0.1:5000/api/v1/" ===>> 

![we should add the prefix to the url like this ](image-4.png)

# CHATGPT ANSWER ON WHY WE USE A PREFIX
![CHATGPT ANSWER ON WHY WE USE A PREFIX](image-6.png)    

    tags=["/api/v1"]
)

-------------------------------------------------------------
---------------------------
-------------------------------

here to get the variables from dot env = ![alt text](image-7.png)

-----------------
----------

the order here is very important cuz he's getting vars from hidden donenv before going to base = ![alt text](image-8.png)
-------
--------

async def vs def == ![alt text](image-9.png)

--------
--------
load_dotenv(override=True)  = so we can update the .env content easily ==== we may not use it 

----------
----------
INSTEAD OF DOING THIS : LOOK AT THE NEXT STEP : ![](image-12.png)

Why we use __init__.py in folders? beucase it will indicate into the folder itself like that {we use it so we don't call dic in another dic>...etc} So when we wrote the code of (((from .Datacontrollers import DataController ==== in __INIT__, it will be the first thing the folder will look into))): ![alt text](image-11.png)

----------
-----------


HERE WE HAVE MANAGED TO UPLOAD FILE AND CHECK FOR THE EXTINTION OF IT ![alt text](image-13.png)

POSTED !! ![alt text](image-14.png)

-------
----------

We've also used ENUM ![alt text](image-15.png)

-------------
-------

Now we've uplaoded file and we got the fine name (randomly generated and file path)![alt text](image-16.png)
here too =>![alt text](image-17.png)